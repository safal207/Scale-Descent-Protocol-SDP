[package]
name = "sdp-cli"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
sdp-core = { path = "../sdp-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
